# Realistic benchmark datasets (synthetic)

JSON files here are **100% synthetic**: fake names, emails, phones, and IDs suitable for public repos.

- Regenerate core fixtures: from `packages/core`, `pnpm run datasets:realistic` (runs `generate-fixtures.mjs`).
- Do not commit real customer, healthcare, or production log data.
