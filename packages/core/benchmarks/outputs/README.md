# Generated benchmark artifacts

Files in this directory are **overwritten** when you run `pnpm run bench:realistic` in this package. They mirror the JSON/Markdown at `benchmarks/` root for tooling that prefers an `outputs/` folder.
