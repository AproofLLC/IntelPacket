# Realistic PII-shaped datasets (synthetic)

These fixtures **mimic** PII-heavy shapes using obviously fake values (`@*.invalid`, `555-*`, synthetic IDs).

- Regenerate: from `packages/pii`, `pnpm run datasets:realistic`.
