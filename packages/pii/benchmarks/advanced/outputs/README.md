# Advanced benchmark outputs

Generated by `pnpm run bench:advanced` (or `pnpm run bench:pii:advanced`) in `@intelpacket/pii`:

- `pii-advanced-results.json`
- `PII_ADVANCED_REPORT.md`

Overwritten each run.

**CLI:** `--scale=10k|100k|1m`, `--iterations=N`, optional `--strict-scale`. Without `--strict-scale`, nominal `10k` / `100k` / `1m` scales may run with a **smaller effective user count** so the suite finishes in reasonable time — compare `cli.recordCount` to `cli.scaleRequested` in the JSON.
